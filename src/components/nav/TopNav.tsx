import * as React from 'react';
import { Header } from '../../layouts';

export const TopNav: React.FC<any> = props => {
  return (
    <div style={{ backgroundColor: "#CDF1AF"}}>
      <Header />
    </div>
  )
}